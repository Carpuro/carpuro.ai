# dependencies
node_modules/

# astro build output
dist/
.astro/

# vercel
.vercel/

# dev tooling (screenshots; keep the script)
.tooling/*.png

# env / secrets — never commit real keys; the template (.env.example) is tracked
.env
.env.*
!.env.example

# os / misc
.DS_Store
*.log
npm-debug.log*

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
